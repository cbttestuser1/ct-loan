package com.ct.stepDefinitions;

public class CTeckLoan43ContactInfoNegative {

	// WebDriver driver = Driver.getInstance();
	//
	// CTeckLoan43ContatInfoHome pages = new CTeckLoan43ContatInfoHome();
	// CTContstants url = new CTContstants();
	// WebDriverWait wait = new WebDriverWait(Driver.getInstance(), 10);

	// @Given("^user on the homepage of CT-Loan$")
	// public void user_on_the_homepage_of_CT_Loan() throws Throwable {
	// Driver.getInstance().get(url.QA_URL);
	// WebElement iframe =
	// Driver.getInstance().findElement(By.xpath("//div[@class='qodef-content']//iframe"));
	// Driver.getInstance().switchTo().frame(iframe);
	// }

	// @Given("^users adds
	// \"([^\"]*)\",\"([^\"]*)\",\"([^\"]*)\",\"([^\"]*)\",\"([^\"]*)\"$")
	// public void users_adds(String Address, String AddressLin2, String City,
	// String State, String ZipCode)
	// throws Throwable {
	//
	// pages.Address1.sendKeys(Address);
	// pages.Address2.sendKeys(AddressLin2);
	// pages.City.sendKeys(City);
	// pages.State.sendKeys(State);
	// pages.ZipCode.sendKeys(ZipCode);
	// }

	// @Given("^user picks a Country$")
	// public void user_picks_a_Country(String country) throws Throwable {
	// pages.selectFromDropDown(pages.Country, country);
	//
	// }

	// @Given("^user enters Personal Phone Number \"([^\"]*)\"$")
	// public void user_enters_Personal_Phone_Number(String PPCCode, String PP1,
	// String PP2) throws Throwable {
	// pages.PersonalPhoneCountryCode.sendKeys(PPCCode);
	// pages.PersonalPhoneFirst.sendKeys(PP1);
	// pages.PersonalPhoneSecond.sendKeys(PP2);
	//
	// }
	//
	// @Given("^user enters Work Phone Number \"([^\"]*)\"$")
	// public void user_enters_Work_Phone_Number(String WPCCode, String WP1,
	// String WP2) throws Throwable {
	// pages.WorkPhoneCountryCode.sendKeys(WPCCode);
	// pages.WorkPhoneFirst.sendKeys(WP1);
	// pages.WorkPhoneSecond.sendKeys(WP2);
	// }
	//
	// @Given("^user enters Extension \"([^\"]*)\"$")
	// public void user_enters_Extension(String Ext) throws Throwable {
	// pages.Extension.sendKeys(Ext);
	// }
	//
	// @Given("^user enters Personal Email \"([^\"]*)\"$")
	// public void user_enters_Personal_Email(String pEmail) throws Throwable {
	// pages.PersonalEmail.sendKeys(pEmail);
	// }
	//
	// @Given("^user enters Work Email \"([^\"]*)\"$")
	// public void user_enters_Work_Email(String wEmail) throws Throwable {
	// pages.WorkEmail.sendKeys(wEmail);
	// }

	// @Given("^user should click on Next button$")
	// public void user_should_click_on_Next_button() throws Throwable {
	// pages.clickOnNextButton();
	// }

	// @Then("^user need to assert that he is in Employment Information Page$")
	// public void
	// user_need_to_assert_that_he_is_in_Employment_Information_Page() throws
	// Throwable {
	// wait.until(ExpectedConditions.visibilityOf(pages.ContactText));
	// pages.assertIsTextDisplayed(pages.ContactText);
	// System.out.println(pages.ContactText.getText());
	// }
}
